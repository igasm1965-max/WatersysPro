
This directory is intended for project header files.

A header file is a file containing C declarations and macro definitions
to be shared between several project source files. You request the use of a
header file in your project source file (C, C++, etc) located in `src` folder
by including it, with the C preprocessing directive `#include'.



















https://gcc.gnu.org/onlinedocs/cpp/Header-Files.html* Computed Includes* Once-Only Headers* Include Operation* Include SyntaxRead more about using header files in official GCC documentation:In C, the convention is to give header files names that end with `.h'.find one copy will result in inconsistencies within a program.finding and changing all the copies as well as the risk that a failure tonew version when next recompiled. The header file eliminates the labor ofplace, and programs that include the header file will automatically use thein only one place. If they need to be changed, they can be changed in oneand error-prone. With a header file, the related declarations appearinto each source file that needs it. Such copying would be time-consumingnIncluding a header file produces the same results as copying the header file